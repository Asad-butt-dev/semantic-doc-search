services:
     backend:
        build: 
            context: .
            dockerfile: dockerfile
        restart: unless-stopped
     frontend: 
         build: 
            context: ./frontend 
         ports: 
          - "80:80"
         depends_on:
           - backend 
         restart: unless-stopped